#! /bin/sh

if [ ! -e /usr/lib/shutorial/bin ] ; then
  echo "This command is dangerous to execute out of the shutorial environment. Stop being foolish and get some sleep. Now."
  exit 1
fi

echo "ce fichier est inutile, mais il existe." > OK

# SHTRL_INCLUDE plus.sh .
# SHTRL_INCLUDE animaux .

# SHTRL_INCLUDE shtrl-check.sh /usr/lib/shutorial/bin shtrl-check
